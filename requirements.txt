# Habitar 7.2 GraphML pipeline - pinned dependencies (Python 3.12)
# Install:  pip install -r requirements.txt
# Note: torch / torch_geometric wheels are platform-specific; on some machines
# you may need the official PyTorch index (https://pytorch.org/get-started).

# Core geometry + graph engine
topologicpy==0.9.43

# Data / numerics / plotting
numpy==2.4.4
pandas==2.3.3
matplotlib==3.11.0

# Graph ML (pretrained MSD node classifier)
torch==2.12.0
torch_geometric==2.8.0

# Notebook execution (Restart & Run All / headless via nbconvert)
ipykernel==7.2.0
jupyter_core==5.9.1
jupyter_client==8.8.0
nbconvert==7.17.1
nbclient==0.11.0
nbformat==5.10.4
